# Railway PostgreSQL 연결 정보
# 이 파일의 내용을 복사하여 .env.local 파일을 만들고 실제 값으로 변경하세요
# .env.local 파일은 .gitignore에 포함되어 있어 Git에 커밋되지 않습니다

# 방법 1: 개별 변수 사용 (권장)
RAILWAY_DB_HOST=containers-us-west-123.railway.app
RAILWAY_DB_PORT=5432
RAILWAY_DB_NAME=railway
RAILWAY_DB_USER=postgres
RAILWAY_DB_PASSWORD=your-password-here

# 방법 2: 전체 DATABASE_URL 사용 (선택사항)
# RAILWAY_DATABASE_URL=postgresql://postgres:password@host:port/railway

# Railway에서 DATABASE_URL 확인 방법:
# 1. Railway 대시보드 → PostgreSQL 서비스 선택
# 2. Variables 탭에서 DATABASE_URL 확인
# 3. 형식: postgresql://user:password@host:port/database
# 4. 이를 개별 변수로 분리하여 위에 입력
