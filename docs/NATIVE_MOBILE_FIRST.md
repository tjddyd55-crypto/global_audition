# Native mobile-first (frontend/mobile)

웹·백엔드 비즈니스 로직을 SSOT로 유지한 채, 1차 클라이언트를 네이티브 앱으로 전환한다.

- 구현 위치: `frontend/mobile` (기존 Expo 앱 확장)
- 상세 결정: `frontend/mobile/NATIVE_ARCHITECTURE.md`
- PC/웹(`frontend/web`)은 기획사 관리·생성·폴백으로 유지한다.
- 프로덕션 배포, DB 마이그레이션, 시크릿 변경은 이 작업 범위가 아니다.
