# =============================================================================
# Frontend Web - 환경 변수 예시 (복사 후 .env.local 등에 설정)
# =============================================================================
# 필수 (빌드 타임 가드: src/lib/env.ts)
NEXT_PUBLIC_API_URL=

# 문서상 동일 역할 (선택) - 현재 프로젝트는 NEXT_PUBLIC_API_URL 사용
# NEXT_PUBLIC_API_BASE_URL=

# 선택
# NEXT_PUBLIC_LOCALE=ko
# NEXT_PUBLIC_USE_GATEWAY=false
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
