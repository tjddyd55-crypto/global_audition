# =============================================================================
# 사용 중지 — spring.profiles.active=production 으로 로드하지 마세요.
# Railway·운영은 application.yml + 환경변수만 사용 (SPRING_PROFILES_ACTIVE 삭제).
# DB: SPRING_DATASOURCE_URL 또는 PGHOST/PGPORT/PGDATABASE/PGUSER/PGPASSWORD
# S3: AWS_BUCKET, AWS_REGION, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY
# =============================================================================
# 유효한 YAML 자리표시자(실제 설정 없음)
audition:
  deprecated-production-file-marker: true
